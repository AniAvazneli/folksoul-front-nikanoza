export type LoginFormValues = {
  name: string;
  password: string;
};

export type MusicianFormValues = {
  name: string;
  instrument: string;
  orbitLength: string;
  color: string;
  biography: string;
};
